{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block stylesheets %}
   
{% endblock %}

{% block javascripts %}
    <script>
        console.log('hello')
    </script>
{% endblock %}

{% block body %}
<div class="container text-white">
    <h1>Hello {{ controller_name }}! ✅</h1>

    {% if name == "Jordan" %}
        <h2>Hello {{ name }}</h2>
    {% else %}
        <h3>Bonjour {{ name }}</h3>    
    {% endif %}    

    <h4>numéro: {{number}}</h4>

    {# {% for c in 0..15 %}
        <div>{{c}}</div>
    {% endfor %} #}

    {# commentaire #}

    {% for key, value in joueurs %}
        {% if loop.last %}
            <div style="color:red;">{{ loop.index0 }} - {{key}} - {{value}}</div>
        {% else %}
             <div>{{ loop.index0 }} - {{key}} - {{value}}</div>    
        {% endif %}
       
    {% endfor %}

    {% set a = "value" %}

    <div>{{a}}</div>
    <div>{{path('testpage')}}</div>

    <a href="{{path('testpage')}}">lien vers page test</a><br>
    <a href="{{path('new_blog')}}">lien vers new blog</a>
    <a href="{{path('blog', {'page':3, 'id':12})}}">Vers produit id 12</a>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>F:/SymfoStudentDoc2025/demo/src/Controller/HomeController.php</code></li>
        <li>Your template at <code>F:/SymfoStudentDoc2025/demo/templates/home/index.html.twig</code></li>
    </ul>
    <img src="images/wall.jpg" alt="image">
    <img src="{{asset('images/wall.jpg')}}" alt="image">
    <img src="{{asset('images/Lebron.png')}}" alt="image">

</div>
{% endblock %}
